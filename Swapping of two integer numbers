#include <stdio.h>

int main() {
    int a,b;
    printf("enter two integers:");
    scanf("%d%d",&a,&b);
    printf("before swapping: a=%d, b=%d");
    a=a+b;
    b=a-b;
    a=a-b;
    printf("after swapping: a=%d,b=%d");
    

    return 0;
}
